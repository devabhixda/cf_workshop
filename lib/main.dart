
import 'package:flutter/material.dart';
import './Done.dart';

void main() => runApp(cf_workshop());

class cf_workshop extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Done(),
      routes: {
      },
    );
  }
}
